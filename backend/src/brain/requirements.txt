# BotFC Brain – Python 2.7 dependencies
#
# pynaoqi is NOT on PyPI.  Install it from the NAOqi SDK bundle:
#   https://community.aldebaran.com/en/resources/software/language/en-gb
# Then add the site-packages path to PYTHONPATH before running:
#   export PYTHONPATH=/opt/aldebaran/lib/python2.7/site-packages
#
# The remaining packages are standard library (already on the robot) or
# installable via pip2 if you need them on a dev machine:

# Standard library (no install needed on the robot):
#   sys, os, time, math, json, signal, argparse, threading, struct, socket,
#   hashlib, base64

# Optional – only needed if you run the brain on a dev machine for unit tests:
# numpy==1.16.6        # last release supporting Python 2.7
# opencv-python==4.2.0.34   # last cv2 wheel for Python 2.7
